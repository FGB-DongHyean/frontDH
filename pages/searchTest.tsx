import SearchBar from '@/components/search/SearchBar';
import { NextPage } from 'next';

// searchBar 확인
const searchTest: NextPage = () => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default searchTest;
